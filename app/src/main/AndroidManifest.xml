<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.comtest.olandroots">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.ScheduleActivity" />
        <activity android:name=".pages.ContactActivity" />
        <activity android:name=".pages.AboutActivity" />
        <activity android:name=".pages.InfoActivity" />
        <activity android:name=".pages.MapActivity" />
        <activity android:name=".pages.HelpActivity"></activity>
    </application>

</manifest>